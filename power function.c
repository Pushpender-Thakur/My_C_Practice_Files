//see the use of pow function; pow = power
#include <stdio.h>
#include <math.h>
int main (void)
{
	int a;
	a = pow (3,2);// 3 to the power 2 which is 9 of course :)
	printf("%d",a);
	return 0;
}
